create table MECHANICS
(
    ID             BIGINT identity
        primary key,
    FIRST_NAME     VARCHAR(100),
    PATRONYMIC     VARCHAR(100),
    LAST_NAME      VARCHAR(100),
    HOURLY_PAYMENT INTEGER
);